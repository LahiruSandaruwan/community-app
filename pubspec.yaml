name: educonnect
description: A community chat platform for tutors and students - Educational discussion groups similar to WhatsApp Communities

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase Core (Required for all Firebase services)
  firebase_core: ^2.24.2

  # Firebase Authentication
  firebase_auth: ^4.15.3

  # Cloud Firestore for real-time database
  cloud_firestore: ^4.13.6

  # Firebase Storage for profile pictures
  firebase_storage: ^11.5.6

  # Firebase Cloud Messaging for push notifications
  firebase_messaging: ^14.7.9

  # State Management
  provider: ^6.1.1

  # UI Components
  cupertino_icons: ^1.0.6

  # Image Handling
  image_picker: ^1.0.7
  cached_network_image: ^3.3.1

  # UUID Generator for invite codes
  uuid: ^4.3.3

  # Time formatting
  intl: ^0.18.1

  # Local Storage for offline caching
  shared_preferences: ^2.2.2

  # QR Code generation for invite links
  qr_flutter: ^4.1.0

  # Permission handling
  permission_handler: ^11.2.0

  # URL Launcher for invite links
  url_launcher: ^6.2.4

  # Loading indicators
  flutter_spinkit: ^5.2.0

  # Toast messages
  fluttertoast: ^8.2.4

  # Network connectivity
  connectivity_plus: ^5.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # Assets
  assets:
    - assets/images/
    - assets/icons/

  # Fonts (Optional - using system fonts for now)
  # fonts:
  #   - family: Roboto
  #     fonts:
  #       - asset: fonts/Roboto-Regular.ttf
  #       - asset: fonts/Roboto-Bold.ttf
  #         weight: 700
